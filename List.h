#pragma once
#include <SFML/Graphics.hpp>

/*
using namespace sf;

struct node {
	Object obj;
	node* next;
};

class List
{
private:
	node* tail;
	node* head;
	
public:
	List();
	void addNode(Object& obj);
	node* getHead()
	{
		return head;
	}
	void display(node* head, RenderTarget& target);
};
*/
